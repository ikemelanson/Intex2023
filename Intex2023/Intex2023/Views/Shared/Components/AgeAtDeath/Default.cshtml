@*@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model IEnumerable<string>

<div class="d-grid gap-2 text-center">

    <a asp-controller="Home" asp-action="BurialRecords" asp-route-ageatdeath="" asp-route-pageNum="1" class="btn @(ViewBag.SelectedAgeAtDeath == null ? "btn-primary" : "btn-outline-secondary")">No Age at death Filtering</a>

    @foreach (string t in Model.Where(s => !string.IsNullOrWhiteSpace(s)))
    {
        <a asp-controller="Home" asp-action="BurialRecords" asp-route-ageatdeath="@t" asp-route-pageNum="1"
           class="btn @(ViewBag.SelectedAgeAtDeath == t ? "btn-primary" : "btn-outline-secondary")">@t</a>
    }
</div>
<br />